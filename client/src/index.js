import React from 'react';
import { render } from 'react-dom';

import { BrowserRouter as Router } from 'react-router-dom';

render(
  <Router>
    {'Put the app component here once we make it - make sure to import'}
  </Router>,
  document.getElementById('root')
);
